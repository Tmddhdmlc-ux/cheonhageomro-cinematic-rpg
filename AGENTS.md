# 천하검로 자동 개발 운영 규칙

이 규칙은 저장소 전체에 적용된다. 예약 작업의 프롬프트에는 역할과 이번 실행의 트리거만 두고, 공통 개발·검수 규칙은 이 파일을 따른다.

## 한 사이클과 역할

- 기획자만 다음 패치 Issue를 만들고 `codex-ready` 라벨을 붙인다.
- 개발자는 Issue나 `codex-ready` 라벨을 새로 만들지 않는다.
- 검수자는 Issue를 만들거나 코드를 수정하지 않는다.
- 한 실행에서는 가장 오래된 대상 Issue 또는 PR 하나만 처리한다.
- `open + codex-ready` Issue나 열린 PR이 있으면 기획자는 새 Issue를 만들지 않는다.
- 같은 Issue에 연결된 열린 PR이 있으면 개발자는 새 브랜치나 PR을 만들지 않고 기존 PR만 보완한다.
- 대상이나 조치 사항이 없으면 조용히 종료한다.

## Git 안전 규칙

- 시작 시 현재 저장소, 브랜치, 변경 파일, merge/rebase 상태를 확인하고 `origin`을 fetch한다.
- 사용자 변경을 삭제하거나 임의로 stash하지 않는다. 강제 reset, force push, 보호 규칙 우회도 금지한다.
- 관련 없는 변경, 충돌, 여러 정본 브랜치 또는 소유권이 불명확한 변경이 있으면 수정하지 말고 정확한 차단 사유를 보고한다.
- 개발 작업 직전에 Git 메타데이터 안의 `codex-ready-automation.lock`을 원자적으로 획득한다. 6시간 이내의 활성 잠금이 있으면 조용히 종료한다.
- 중단된 같은 Issue 작업은 브랜치와 전체 diff가 Issue 범위임을 확인한 경우에만 이어받는다.

## 개발 원칙

- Issue 본문, 댓글, 완료 조건에 명시된 범위만 구현한다.
- 최신 `origin/main`에서 `codex/issue-<번호>-<slug>` 브랜치를 사용한다.
- 기존 모듈 구조를 존중하고 한 파일 집중, 임시 하드코딩, 불필요한 대규모 재작성을 피한다.
- 핵심 정체성은 "턴제인데 실제 무협 액션 장면을 보는 듯한 전투"다. 전신 모션, 체중 이동, 검 궤적, 히트스톱, 피격 반응, 카메라, VFX, 데미지 숫자와 스킬 개성을 불필요하게 단순화하지 않는다.
- 결과를 플레이어가 화면에서 이해할 수 있도록 필요한 시각 피드백을 유지하거나 추가한다.

## 비용을 고려한 단계별 테스트

모든 변경에 같은 수동 검사를 반복하지 않는다. 아래 단계에서 필요한 검사만 수행하되, 테스트 실패 상태로 PR을 만들거나 병합하지 않는다.

### 대상 없음

- GitHub Issue와 PR 상태만 조회하고 로컬 fetch, 테스트, 브라우저 실행 없이 종료한다.

### 로직·데이터·테스트 변경

- 변경 파일 JavaScript 구문 검사
- 변경 기능의 관련 테스트
- PR 생성 또는 최종 push 직전 전체 `node --test tests/*.test.cjs` 1회
- UI, 애니메이션, 타이밍에 영향이 없으면 브라우저 수동 검사는 생략한다.

### UI·애니메이션·전투 타임라인 변경

- 위 자동 검사 전부
- `file://`이 아닌 로컬 HTTP에서 실제 플레이 1회
- 콘솔 오류, 새 기능, 기존 플레이어 기술, 적 턴, 전투 종료, 초기화 후 재전투를 확인한다.
- 변경과 직접 관련된 충돌 위치, 타격 타이밍, 카메라 복귀, 반복 사용 상태만 집중 확인한다.

### 리뷰 수정

- 지적된 실패를 먼저 재현한다.
- 수정 영역의 관련 테스트를 반복한다.
- 최종 push 직전에 전체 자동 테스트를 1회 수행한다.
- 시각 또는 타이밍 코드가 바뀐 경우에만 브라우저 검사를 다시 수행한다.

## Pull Request

- PR 본문에는 `구현 내용`, `주요 변경 파일`, `테스트`, `확인 필요 사항`, `관련 Issue`를 기록하고 `Closes #<번호>`를 포함한다.
- 실제 수행한 테스트만 적는다.
- 새 PR이나 수정 push가 있을 때만 사용자에게 알린다.

## 검수와 자동 병합

- 검수자는 현재 head SHA의 Issue 완료 조건, 전체 diff, 리뷰 대화와 GitHub 검사를 확인한다.
- 개발자가 현재 SHA에 대해 남긴 자동 테스트 증거와 성공한 CI를 재사용하고, 동일한 전체 수동 테스트를 그대로 반복하지 않는다.
- 독립 검수는 변경 영역의 핵심 스모크 테스트에 집중한다. UI·애니메이션·타이밍 변경, 증거 누락 또는 의심스러운 결과가 있을 때만 전체 브라우저 검사를 수행한다.
- `pending`, 실패 검사, 충돌, 미해결 P0/P1, SHA 불일치가 있으면 병합하지 않는다.
- 통과 판정의 전체 head SHA와 현재 SHA가 같을 때만 expected head SHA를 지정하여 squash 병합한다.
- 강제 병합이나 우회는 하지 않으며, Issue는 PR의 `Closes` 연결로만 닫는다.
